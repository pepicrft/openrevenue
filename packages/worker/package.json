{
  "name": "openrevenue-worker",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --local --persist-to .wrangler/state",
    "deploy": "wrangler deploy",
    "lint": "echo 'no lint configured'",
    "seed": "wrangler d1 execute openrevenue --local --file ./seed.sql",
    "test": "vitest"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "hono": "^4.5.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "wrangler": "^4.59.2"
  }
}
